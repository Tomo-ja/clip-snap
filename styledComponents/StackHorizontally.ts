import { styled } from '@mui/material/styles'
import { Box } from '@mui/material'


const StackHorizontally = styled(Box)(({ theme }) => ({
	display: 'flex',
	alignItems: 'center'
}))

export default StackHorizontally